# Optional overrides. Leave these empty to reuse DiscoveryOS OpenRouter settings.
OPENROUTER_API_KEY=
DEEPAGENT_MODEL=
DISCOVERYOS_ENV_FILE=/Users/qszhang/Documents/codex/DiscoveryOS/.env.local

# Optional: point the coordinator/workspace APIs at an untracked per-machine
# resource file instead of the repository default.
# INTERNAGENT_RESOURCES_FILE=internagent.resources.local.json
